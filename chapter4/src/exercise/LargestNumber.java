package exercise;
/*
*  this application prompts user to enter numbers
* then prints the largest number
* */

public class LargestNumber {
    private int largestNumber;

    public int totalUnits(int number) {
        if (number > 0) {
            largestNumber = number;
        }
        return largestNumber;
    }
}
